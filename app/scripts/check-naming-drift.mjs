#!/usr/bin/env node
// R002 — naming-singular-plural guard (stub; upgraded in R002 commit).
//
// 当前是占位脚本,在 commit 3 会被替换为完整实现。本 stub 仅保证
// `pnpm rule:check-naming-drift` 可被独立调用且 exit 0(尚未生效)。

console.log('[R002 stub] pending full implementation — see docs/rules/R002-naming-singular-plural.md');
process.exit(0);
