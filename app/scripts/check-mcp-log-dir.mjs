#!/usr/bin/env node
// R003 — mcp-log-dir-must-match guard (stub; upgraded in R003 commit).
//
// 当前是占位脚本,在 commit 3 会被替换为完整实现。本 stub 仅保证
// `pnpm rule:check-mcp-log-dir` 可被独立调用且 exit 0(尚未生效)。

console.log('[R003 stub] pending full implementation — see docs/rules/R003-mcp-log-dir-must-match.md');
process.exit(0);
